[project]
name = "voice-contract"
version = "0.1.0"
description = "Shared pydantic models for the voice-layer <-> CollectiveOS event contract (see /contract at repo root)."
requires-python = ">=3.12"
dependencies = ["pydantic>=2"]

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"
